steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - ${_IMAGE}
      - -f
      - deploy/Dockerfile.api
      - .
images:
  - ${_IMAGE}
options:
  logging: CLOUD_LOGGING_ONLY
